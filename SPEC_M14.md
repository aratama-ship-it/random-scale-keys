# M14 仕様 — 隣接タイルの同度数を解消する（app v0.18.0）

作成: 2026-09-05 Claude（設計・アルゴリズムはシミュレーション済み）。実装: Codex。検証: Claude → 本人（耳・目）。

本人指摘（2026-09-05、スクリーンショット添付）: ホーム段で「6」「6」の2タイルが横に並んでいた（同じ度数が隣接）。「これはやめたい」。

## 0. 実測（Claude が実装前に確認済み）

現行 `createLayout`（v0.17.0）で、`KEY_ROWS` の同じ行内・隣接タイルの度数が一致する件数を21音階×2世界×10 seed（420通り）で数えたところ、
**合計1,274件、97.1%の配置で1件以上**発生していた（M13 で度数の偏りが増えた影響）。

対策として「行内で隣接する2タイルの (role, degree) のペアを、キーボード上の別のキーと丸ごと入れ替える」修正を
Node で試作し、同じ420通りで検証したところ **全件 0 件に解消**した。役割ごとの総数・役割ごとの度数分布・効果の内訳・
ホーム段（ASDFGHJKL）が全部 `none` であることは、修正の前後で完全に不変であることも確認済み（値の集合として比較。
JSONの文字列比較はキー順序で揺れるため使わない）。以下はこのアルゴリズムをそのまま仕様化したもの。

## 1. 設計の核

- `createLayout` の最後（`keys` を組み立てた直後、`return` の前）に**後処理**として `declumpAdjacentDegrees(keys)` を挟む。
- **role と degree のペアを丸ごと入れ替える**（effect と octave はそのキーに残す）。理由: 同じ role 内だけで
  入れ替えようとすると、そもそも同じ度数がその role に偏っている場合（例: ionian の tension は度数4が6枚・7が1枚）に
  交換相手が見つからず解消できない。role をまたいだ交換なら、role別の合計数・度数分布は保たれたまま
  （どのキーがどの role/degree を持つかが入れ替わるだけ）解消できる。
- **決定論**: 乱数は使わない。候補の探索順は `KEY_CODES`（A→Z）の固定順で、最初に条件を満たした候補を採用する
  「同じ seed なら毎回同じ結果」を満たす。

## 2. `prototype/gravity.mjs` に追加する関数

```js
function rowNeighbors(code) // その行の左右の隣接キーコードを返す（端は片側のみ）。KEY_ROWS から求める
function declumpAdjacentDegrees(keys) // keys を直接書き換えて返す
```

`declumpAdjacentDegrees(keys)` のアルゴリズム:

1. `KEY_ROWS` の各行について、隣接ペア `(a, b)`（行内で連続する2キー）を列挙する。
2. 最大 **6パス**繰り返す。1パスにつき: 全ての隣接ペアを順に見て、`keys[a].degree === keys[b].degree` なら以下で `b` を直す。
   - `KEY_CODES` を順に走査し、`c`（`c !== a && c !== b`）について次をすべて満たす最初の候補を採用する:
     - 新しい `b` の度数（＝現在の `keys[c].degree`）が `keys[a].degree` と異なる（直接の衝突を解消する）
     - `b` の**もう一方の隣人**（`a` を除く。行の端なら存在しない）の度数と、新しい `b` の度数が異なる
     - `c` 自身の行内隣人（`b` は除いて比較。`b` との重複は次の条件でカバー）の度数と、新しい `c` の度数（＝現在の `keys[b].degree`）が異なる
     - `c` の隣人に `b` が含まれ、かつ交換後 `newC === newB` になる場合は不採用（`b`-`c` 自体が隣接同士で無意味な交換になるケースを除く）
   - 候補が見つかったら `keys[b]` と `keys[c]` の **role と degree だけ**を交換する（effect・octave はそのまま）。
     `midi` は交換後の `role`／`degree` と、そのキーが元々持っていた `octave` から
     `midiForDegreeFromRoot(rootMidi, scaleId, degree, octave)` で再計算する。
   - 候補が見つからなければ、そのペアは直さず次のペアへ（次のパスで解消される場合がある）。
   - 1パスで1件でも直したら、次のパスへ進む。1件も直せなかったパスがあれば打ち切ってよい。
3. 全ペアを走査し終えたら `keys` を返す（引数を破壊的に更新している。呼び出し側は戻り値を使う）。

`createLayout` からの呼び出し: `return` 文の直前に `declumpAdjacentDegrees(keys);` を呼ぶ（`keys` はミューテートするのでそのまま使う）。

## 3. 版・記録

- 版表示 **v0.18.0**（`app/index.html` の title と `.version`、対応するテスト）。`engine` は `accomp-v5` のまま。
- README: 「隣接するタイルは同じ度数にならない（役割・度数を丸ごと入れ替えて解消。効果・オクターブは動かさない）」を1行追記。
- **配置の規則が変わるため、同じ seed でも v0.17.0 以前とは配置が違う**（音階・和音・伴奏・重力・リズム・SFX・音色の規則は不変）。

## 4. テスト（`prototype/tests/gravity.test.mjs` に追加）

1. `declumpAdjacentDegrees` の単体テスト: 意図的に隣接同度数を作った小さな `keys` オブジェクト（2〜3件の衝突を含む）を渡し、
   戻り値で全衝突が解消されていること。ホーム段の `effect` と各キーの `octave` が変化しないこと。
2. 21音階 × 2世界 × 5 seed（`[1, 42, 777, 12345, 99999]`）で `createLayout` を呼び、`KEY_ROWS` の全隣接ペアで
   `degree` が一致する組が **0 件**であること。
3. 同じ範囲で、修正前後の（今回は `createLayout` 自体に組み込むので「前」は取れない。代わりに）
   **役割ごとのキー数**（`allocateKeys` の値と一致）、**効果の内訳**（`EFFECT_COUNTS` と一致）、
   **ホーム段が全部 `none`**、が引き続き成り立つこと（既存テストの延長でよい）。
4. 決定論: 同じ seed・同じ引数で `createLayout` を2回呼んで完全一致。
5. seed 42 の固定期待値を使う既存テストがあれば、実装後の実際の出力に更新する（削除しない）。

## 5. 完了条件

- テスト全通過、`node --check`、**ブラウザ console error 0**、v0.18.0 表示
- Claude 実測: 21音階×2世界×複数 seed で隣接同度数 0 件、役割・効果の内訳が不変、画面で目視して隣接する2タイルの数字が
  一致していないことを確認
- 音の良し悪しは対象外（配置のみの変更。音色・伴奏は不変）
