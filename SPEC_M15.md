# M15 仕様 — 度数の全体バランス（度数4への偏りを解消する）（app v0.19.0）

作成: 2026-09-05 Claude（設計・アルゴリズムはシミュレーション済み）。実装: Codex。検証: Claude → 本人（目）。

本人指摘（2026-09-05、スクリーンショット添付）: 1段目に「6,4,6,2,4,4,5,1,3,4」が並び、**4が4枚、1が1枚**。「4がやたら多くて1が少ないバランスはよくない」。

## 0. 原因（Claude 実測）

M13 で「度数7は1キーだけ」にした結果、その役割（多くの7音階で tension、いくつかで floating）に残る度数がもう1つしかない音階
（例: ionian の tension = [4,7]）では、余った枠が**全部その1つの度数**に集中する。実測（21音階×2世界×5 seed）で、
ionian は度数4が6キー・他の度数が3〜4キーという偏りが常に出ていた。

## 1. 設計の核（シミュレーション済み）

**度数ごとのキー数を、役割をまたいで26キー全体で均等にする。** 現行は「役割ごとのキー数を先に固定 → その役割内の度数へ配分」
という2段構えのため、役割内の度数が少ないと偏りが生まれる。今回は「まず26キー全体で度数ごとの目標キー数を決め（度数7だけ1に抑える）、
役割ごとのキー数はその結果として決まる」向きに変える。

- 7音階（度数1〜7）の目標キー数は `base = floor(26/7) = 3`、余り `26 - 3*7 = 5` を度数1から順に+1 → 度数1〜5が4枚、6〜7が3枚。
- 度数7だけ1枚に抑え、浮いた `3-1=2` 枚を度数1・2に+1ずつ配る（度数1から順に、余りが尽きるまで）。
- **結果、7音階12種すべてで目標は同じ**: `{1:5, 2:5, 3:4, 4:4, 5:4, 6:3, 7:1}`（21音階中の7音階12種で共通。Claude が実測済み）。
- 五音・六音音階（度数7が存在しない）は対象外。現行どおり。
- **役割は度数から自動的に決まる**（`roleForDegree`）。役割ごとのキー数は音階によって変わってよい
  （例: ヒジャーズは floating=[7] のみなので、floating が 3→1、tension（度数2,4,6）が 11→12 になる。これは仕様どおりで、
  音階ごとに「緊張しやすさ」が変わる副作用として受け入れる）。

## 2. 実装（`prototype/gravity.mjs`）

### 2.1 `degreeTargets(scaleId)` → `{ [degree]: count }`

- `N = getScale(scaleId).intervals.length`。`N < 7` なら `null` を返す（対象外）。
- `base = Math.floor(KEY_CODES.length / N)`、`remainder = KEY_CODES.length - base * N`。度数1..N に `base` を割り当て、
  度数1から順に `remainder` 個へ+1。
- 度数7の目標が1より大きければ、`freed = target[7] - 1`、`target[7] = 1`。残り度数（1..N のうち7を除く）へ、
  同じ「基本値＋度数1から順に余りを配る」方式で `freed` を配り直す。
- `KEY_CODES.length` を直書きしない（将来キー数が変わっても動くように）。

### 2.2 `createLayout` の変更

現行の「役割を先に決めてから度数を配る」処理（`roles = shuffled(...)`、`degreesByRole = ...`、`balancedDegrees` を使う部分）を、
以下に置き換える:

1. `degreeTargets(scaleId)` が `null`（五音・六音音階）なら**現行のアルゴリズムをそのまま使う**（役割ごとの配分。変更なし）。
2. `null` でなければ:
   - 度数ごとの目標枚数から、`degreePool`（度数の配列。度数dが`target[d]`回出現。長さ26）を作る。
   - `shuffled(degreePool, random)` で並びをシャッフルする（既存の `shuffled` 関数を使う。乱数消費は1回のシャッフル分）。
   - `KEY_CODES` を順に走査し、`degreePool[index]` から度数を取り、`role = roleForDegree(scaleId, degree)` で役割を求める。
   - 効果・オクターブの割当ロジック（`effects` のシャッフル、`octaveRoll` の乱数消費）は**現行のまま**（乱数消費の順序は
     「役割・度数のシャッフル→効果のシャッフル→オクターブ」のままにする。具体的には度数プールのシャッフルを、
     現行の `roles = shuffled(...)` の位置で行う一回の乱数消費に置き換える形にする）。
3. `declumpAdjacentDegrees`（M14）は**そのまま後段で実行する**（度数プールをシャッフルしても、行内隣接で同じ度数が
   偶然並ぶことはあるため、既存の後処理は必要）。

## 3. 版・記録

- 版表示 **v0.19.0**（`app/index.html` の title と `.version`、対応するテスト）。`engine` は `accomp-v5` のまま。
- README: 「度数の配分は26キー全体で均等になるよう決める（度数7だけ1枚）。役割ごとのキー数は音階によって変わる」を1行追記。
- 配置の規則が変わるため、同じ seed でも v0.18.0 以前とは配置が違う（音階・和音・伴奏・重力・リズム・SFX・音色の規則は不変）。

## 4. テスト（`prototype/tests/gravity.test.mjs` に追加・更新）

1. `degreeTargets`: ionian で `{1:5,2:5,3:4,4:4,5:4,6:3,7:1}`（合計26）。五音音階（例 `major_pentatonic`）で `null`。
2. 7音階12種すべてで `degreeTargets` の合計が26であること、度数7が1であること。
3. 21音階×2世界×5 seed（`[1, 42, 777, 12345, 99999]`）で `createLayout` を呼び、7音階では**度数ごとのキー数が
   `degreeTargets` と一致**すること。五音・六音音階では**現行どおり**（このM15の変更で影響を受けない。既存テストのまま）。
4. 同じ範囲で、`declumpAdjacentDegrees` 適用後も行内隣接の同度数が **0件**（M14 の不変条件を維持）。
5. 効果の内訳（`EFFECT_COUNTS`）とホーム段（ASDFGHJKL 全部 `none`）は不変（M13 の不変条件を維持）。
6. 決定論: 同じ seed・同じ引数で2回呼んで完全一致。
7. 既存テストのうち「役割ごとのキー数が `allocateKeys(scale.roles)` と一致する」ものは、7音階については
   **新しい役割ごとのキー数**（`degreeTargets` を `roleForDegree` で集計した値）に更新する（削除しない）。
   五音・六音音階のテストは変更不要。

## 5. 完了条件

- テスト全通過、`node --check`、**ブラウザ console error 0**、v0.19.0 表示
- Claude 実測: 7音階12種で度数ごとのキー数が `{1:5,2:5,3:4,4:4,5:4,6:3,7:1}` と一致、隣接同度数0件、
  五音・六音音階は今回の変更で挙動が変わらないこと
- 音の良し悪しは対象外（配置のみの変更）。画面で「4が突出して多い」ような偏りが解消されていることを本人が見て判断
