# M7 仕様 — 伴奏の作り直し（app v0.8.0）

作成: 2026-09-04 Claude（設計）。実装: Codex。検証: Claude（測定）→ 本人（耳）。
本人指摘（2026-09-04）: 伴奏の問題は4つとも。**①弾いた音と和音が合わない ②進行が単調・不自然 ③ベースが動かない・重い ④音量バランス・音色**。
本人決定: **メジャー（daylight+ionian）も一緒に良くする**（7音階も変更対象。「既存の音を変えない」という M5/M6 の制約は今回は外す）。

## 0. 設計の核

**和音を固定進行から「弾いた音に追従する選択」へ変える。** 合わない原因は、和音が演奏と無関係に決まっていたこと。
進行の骨格（機能のバイアス）は残しつつ、直近に弾かれた音を含む和音を選ぶ。これで①と②を同時に解く。

すべて決定論的（乱数を使わない）。同じイベントログからは常に同じ伴奏が出る（再生・WAV・ステム・MIDI が一致する条件）。

## 1. 和音の選択（`prototype/gravity.mjs` に純粋関数として）

### 1.1 決定のタイミング
**各小節の最後の8分音符の位置で、次の小節の和音を決めて確定する**（`chooseChord`）。
確定した和音は次の小節の頭から使う。ベースの経過音（§3）も同じ確定値を使うため、食い違わない。
小節0（テイク開始）は無条件に tonic。

### 1.2 音の記憶 `noteMemory`
- 直近に発音された度数の重み付きヒストグラム。イベントごとに `weight = velocity * exp(-Δbeats / 4)`（Δbeats は決定時点までの経過拍）。
- 対象は `kind === "press"` と応答音。8拍より古いものは捨てる。

### 1.3 候補と採点 `scoreChord(candidateDegree, ctx)`
候補＝音階の各度数 d（1..N）の上に積んだ和音（構成音の作り方は現行の M6 規則のまま）。

```
score = fit + functionBias + voiceLeading + repeatPenalty + tensionBias
```

| 項 | 値 |
|---|---|
| `fit` | Σ（記憶の重み × (和音に含む度数なら +1.0、含まない度数なら −0.5)） |
| `functionBias` | §2 の表（セクションと小節位置で決まる）。該当する機能なら +0.6、tonic は常に +0.15 の下駄 |
| `voiceLeading` | −0.04 × （前の和音の声部から動く半音の合計。§4 の配置で計算） |
| `repeatPenalty` | 直前と同じ和音で、かつ2小節連続していたら −0.5（3小節目以降も −0.5） |
| `tensionBias` | T≥0.6 なら dominant に +0.5／T<0.3 なら tonic に +0.35 |

最大スコアを選ぶ。同点は度数の小さい方。**着地（resolution）が起きた小節は無条件で tonic**（現行の規則を維持）。

## 2. 進行の骨格（`functionBias` の対象。セクション×小節位置）

小節位置 = そのセクション内での小節番号 mod 4。

| セクション | 0 | 1 | 2 | 3 |
|---|---|---|---|---|
| `intro` | tonic | tonic | tonic | subdominant |
| `a` | tonic | tonic | subdominant | dominant |
| `b` | submediant | subdominant | tonic | dominant |
| `outro` | subdominant | tonic | dominant | tonic |

- `tonic` / `subdominant` / `dominant` / `submediant` の度数は M6 の `chordDegrees` をそのまま使う。
- 骨格は**バイアスであって固定ではない**。弾いた音が強く別の和音を支持すればそちらが選ばれる。

## 3. ベース（`prototype/synth.js` / `render.js`）

小節内の配置（拍は1始まり、16分グリッド）:

| 位置 | 音 | 備考 |
|---|---|---|
| 1拍目 | 和音の根音（オクターブ2） | gain 0.9 |
| 2拍目裏（1.5拍） | 休符 | |
| 3拍目 | 和音の**五度**（無ければ三度）。T≥0.30 なら根音の1オクターブ上 | gain 0.75 |
| 4拍目裏（4.5拍） | **次の小節の根音への経過音** | gain 0.7、§3.1 |

### 3.1 経過音 `approachDegree(nextRootSemitone, scale)`
次の根音に対して、音階内の音のうち **半音差が1〜2で最も近いもの**を選ぶ（下からを優先、無ければ上から）。
該当が無ければ次の根音そのもの（同音の先取り）。オクターブは3拍目と同じ帯。

### 3.2 音の作り
- 現行の sine + square(0.15) を、sine + triangle(0.10) に変え、音長を 0.4s → **0.28s**、release を短くして輪郭を出す。
- 低域の整理: ベースに highpass 45Hz を入れる（サブの濁りを取る）。

## 4. パッドの声部連結（voice leading）

- 音域窓 MIDI **55〜79**。前の声部（3音、昇順）に対し、新しい和音の各構成音を「対応する位置の前の音に最も近いオクターブ」に置く。
- 最初の和音は根音を窓の下端付近（root+12k が 55 以上で最小）に置いて積む。
- 上下の広がりが長10度（16半音）を超えたら、最上声部を1オクターブ下げる。
- セクション `b` では3拍目にパッドを再打鍵（gain ×0.6）。他のセクションは小節頭のみ（現行どおり）。

## 5. 音量バランスと音色

現行の実測（16小節・128打鍵）: lead ピーク 0.395 ／ accomp ピーク 0.923 ／ fx 0.091。**伴奏が主役より大きい。**

| 対象 | 変更 |
|---|---|
| `leadBus` gain | 1.0 → **1.25** |
| `accompBus`（パッド・ベース・ドラム）gain | 1.0 → **0.58** |
| パッドの lowpass | daylight 2500Hz → **2000Hz**／night 1800Hz → **1500Hz** |
| ハット gain | 現行 ×**0.6** |
| キック | 長さ 0.12s → 0.10s、クリックの gain 0.5 → 0.35 |
| スネア | gain 0.35 → 0.30 |
| リバーブ送り | パッドのみ ×1.2、ドラムは送らない（現行がドラムも送っているなら外す） |

**測定目標（Claude が検証する）**: 16小節・128打鍵の標準ログで
- ステム lead のピーク **0.55〜0.75**
- ステム accomp のピーク **0.45〜0.65**
- ミックスのピーク **0.85〜0.95**（現行の目標を維持）

目標を外れたら `leadBus` / `accompBus` の2定数だけで合わせる（他の数値は動かさない）。

## 6. 演奏のコツの画面表示（本人決定「表示をしましょう」）

待機画面の凡例の下に1行（`--fs-small`、`--text-sub`）:

```
コツ: 同じキー付近を続けて使うとループ感が出ます。展開したいときは使う位置を少しずつずらします。
```

演奏中・完了時は表示しない（待機画面のみ）。

## 7. 互換性と記録

- イベントログの形式は変えない。ただし**伴奏の生成規則が変わるため、v0.7.1 以前に書き出した JSON を再生すると伴奏が変わる**
  （リードは同じ）。README にその旨を1行。
- ログに `engine: "accomp-v2"` を追加し、古いログ（未設定）でも新規則で再生する（分岐は作らない。単純さを優先）。
- 版表示 v0.8.0。

## 8. テスト

1. `noteMemory`: 重みが `velocity * exp(-Δ/4)`、8拍より古い項が捨てられる
2. `chooseChord`: 記憶が空なら骨格どおり／tonic の構成音だけを弾いた直後は tonic が選ばれる／
   subdominant の構成音だけを強く弾けば骨格が tonic でも subdominant が選ばれる
3. `chooseChord`: 同じ和音が2小節続いた次は `repeatPenalty` により別の和音になりやすい（同点条件を作って確認）
4. `chooseChord`: T=0.7 で dominant が選ばれる／着地した小節は tonic
5. `approachDegree`: 次根音が7半音のとき音階内の5または9（半音差2以内）から下優先で選ぶ／該当なしなら次根音そのもの
6. `voiceLead`: 前の声部 [60,64,67] に対し次の和音 {5,9,0} が [65,69,72] ではなく近い配置になる（総移動量が最小）
7. 決定論: 同じログから `chooseChord` の列が2回とも一致する
8. 既存テストのうち「和音進行が固定表と一致する」ものは新仕様に合わせて更新する（削除しない）

## 9. 完了条件

- テスト全通過、`node --check`、**ブラウザ console error 0**
- §5 の測定目標を満たす（Claude がブラウザで実測）
- 21音階すべてで例外なくレンダーできる
- 同じログの2回レンダーが ±2LSB 以内（決定論）
- 実機未確認の項目は明記
