# M13 仕様 — 配置の規則（ホーム段は効果なし／度数7は1キー）（app v0.17.0）

作成: 2026-09-04 Claude（設計）。実装: Codex。検証: Claude → 本人（耳）。

本人指示（2026-09-04、確認済み）:
- 「**2段目はシンプルな音のみで行きましょう**」→ 文字キーの2段目 **ASDFGHJKL（9キー・ホームポジション）は効果なし**
- 「**7に関してはひとつだけでいいです**」→ **タイルに「7」と出る鍵（度数7）は1キーだけ**

## 0. 設計の核

- 変えるのは `createLayout` の**割当だけ**。音階・和音・伴奏・メロディの重力・リズム・SFX の規則は一切変えない。
- **同じ seed でも v0.16.0 以前とは配置が変わる**（規則が変わるため。承認済みの変更）。決定論は維持する。
- 役割ごとのキー数（`allocateKeys` の ionian 12/7/7 など）と `EFFECT_COUNTS` の内訳（none 11／delay 4／sweep 3／octave 3／stutter 2／arpeggio 3 ＝ 26）は**変えない**。変わるのは「どのキーに置くか」だけ。

## 1. ホーム段は効果なし

- `KEY_ROWS = Object.freeze(["QWERTYUIOP", "ASDFGHJKL", "ZXCVBNM"])` を **`prototype/gravity.mjs` へ移して export** する
  （現在は `app/main.js` の定数）。`app/main.js` はそれを import して使う。画面の描画結果は不変。
- `export const SIMPLE_ROW_INDEX = 1;`（＝ ASDFGHJKL）。`export const SIMPLE_ROW_CODES` として該当9キーの `KeyA` 形式の配列も export してよい。
- `createLayout` は、この段の **9キーに必ず `effect: "none"`** を割り当てる。
- 残り17キーには、残りの効果（none 2／delay 4／sweep 3／octave 3／stutter 2／arpeggio 3 ＝ 17）を従来どおり
  `shuffled(expandedCounts(...), random)` で配る。
- 乱数の消費順は **役割 → 効果 → オクターブ** のまま（順序を入れ替えない）。
- 結果として1段目・3段目は効果が濃くなる（17キー中15キーに効果）。これは意図した対比。

## 2. 度数7は1キーだけ

- `balancedDegrees(degrees, count, random)` に**上限**を渡せるようにし、**度数7はその役割の中で最大1キー**にする。
  余った枠は同じ役割の他の度数へ回す（従来どおり均等配分）。
- **例外**: その役割に属する度数が7だけの場合は上限を適用しない（`hijaz` の floating = [7] のみ該当。適用すると26キーを埋められない）。
- 度数が7未満の音階（五音・六音音階）は対象外（度数7が存在しない）。
- 役割ごとのキー数は変えないので、例えば ionian の tension（度数 4,7・7キー）は **4が6キー／7が1キー**になる。

## 3. 版・記録

- 版表示 **v0.17.0**（`app/index.html` の title と `.version`、対応するテスト）。`engine` は `accomp-v5` のまま。
- README: 「ASDFGHJKL の段は効果なし（確実に素の音が出る段）」「度数7は1キーだけ（`hijaz` を除く）」「配置の規則が変わったので同じ seed でも v0.16.0 以前とは配置が違う」。

## 4. テスト（既存は削除せず更新）

1. 21音階 × 複数 seed で: `ASDFGHJKL` の9キーがすべて `effect === "none"`、26キーの効果の内訳が `EFFECT_COUNTS` と一致、全キーに割当がある
2. 7音階12種（`hijaz` を除く）で度数7のキー数が **1**。`hijaz` は上限が効かず従来どおり。五音・六音音階は度数7が存在しない
3. 役割ごとのキー数が `allocateKeys(scale.roles)` と一致（12/7/7 等）
4. 決定論: 同じ seed・同じ引数で2回呼んで完全一致
5. `KEY_ROWS` を gravity.mjs から export し、`app/main.js` が同じ配列を使っている（キーボードが3段のまま）
6. seed 42 の固定期待値を持つ既存テストは**新しい値に更新**する（実装後に実際の出力で置き換える）

## 5. 完了条件

- テスト全通過、`node --check`、**ブラウザ console error 0**、v0.17.0 表示、画面で ASDFGHJKL の9タイルが全部「—」になっている
- Claude 実測: 21音階×2世界で §4-1〜4 を確認、伴奏（和音・ベース・ドラム）の予約列は配置が変わるぶん以外に規則の差が出ていないこと
- 音の良し悪し（素の段の弾きやすさ、度数7が1キーで足りるか）は本人判定
