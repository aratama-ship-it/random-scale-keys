# M6 仕様 — 五音・六音音階の主和音修正／完了後の設定変更／演奏のコツ（app v0.7.1）

作成: 2026-09-04 Claude（規則は実計算で検証済み）。実装: Codex。検証: Claude → 本人。
本人報告: 「エジプシャンなどでベース（伴奏）が合わない。メジャーは合っていて気持ちいい」「停止後、次を再生する前にスケールやクオンタイズを変えたい」
本人の発見: 「同じキー付近を使い続けるとループ感・楽曲感が出る。展開したければ使う位置をずらす」

## A. 主和音と機能和音の規則を、5〜6音の音階だけ音程基準にする

### A1. 原因（実測）
現行は「主和音＝第1・3・5度（音階内インデックス）」。7音階では三度・五度に当たるが、5音階では飛びすぎる。
エジプシャン [0,2,5,7,10] の第1・3・5度＝C・F・B♭（三度も五度も無い）。メジャーペンタ [0,2,4,7,9] は C・E・A（＝Am/C6）。
安定音・パッド・ベースの根音がここから決まるため、伴奏が「合わない」。

### A2. 規則（N＝音階の音数）
- **N≥7: 現行どおり**（第1・3・5度／subdominant 4・submediant 6・dominant は導音があれば5度なければ最終度）。**12種すべて無変更**。
- **N<7: 音程で選ぶ**（度数1を除く音階音から、目標の半音数に最も近い度数を選ぶ）
  - 三度 = 3 または 4 に最も近い音。同距離なら **低い方**（sus2 を sus4 より優先＝開いた安定音）
  - 五度 = 7 に最も近い音。同距離なら **高い方**（増五度を減五度より優先＝三全音を避ける）
  - 主和音 = {1, 三度, 五度}。**stable はこの3度数**（役割導出の起点が変わる。緊張・浮遊は現行の規則で再導出）
  - subdominant = 5 に最も近い度数／dominant = 7 に最も近い度数／submediant = 9 に最も近い度数
    （いずれも度数1と既に選んだ度数を除外。同距離は低い方）
  - 進行: 基本 `[tonic, tonic, submediant, dominant]`、中緊張は index 1 を subdominant に、高緊張は全 dominant

### A3. 検証済みの結果（2026-09-04 実計算）

| 音階 | 旧主和音 | **新主和音** | 新 stable 度数 | 基本進行の根音 |
|---|---|---|---|---|
| egyptian [0,2,5,7,10] | C-F-B♭ | **C-D-G** | 1,2,4 | C C B♭ G |
| major_pentatonic [0,2,4,7,9] | C-E-A | **C-E-G** | 1,3,4 | C C A G |
| minor_pentatonic [0,3,5,7,10] | C-F-B♭ | **C-E♭-G** | 1,2,4 | C C B♭ G |
| yo [0,2,5,7,9] | C-F-A | **C-D-G** | 1,2,4 | C C A G |
| in_sen [0,1,5,7,8] | C-F-A♭ | **C-F-G** | 1,3,4 | C C A♭ G |
| hirajoshi [0,2,3,7,8] | C-E♭-A♭ | **C-E♭-G** | 1,3,4 | C C A♭ G |
| ryukyu [0,4,5,7,11] | C-F-B | **C-E-G** | 1,2,4 | C C B G |
| blues [0,3,5,6,7,10] | C-F-G | **C-E♭-G** | 1,2,5 | C C B♭ G |
| whole_tone [0,2,4,6,8,10] | C-E-G# | C-E-G#（同じ） | 1,3,5 | C C G# F# |

7音階12種: ionian〜double_harmonic すべて **旧＝新**（変更なし）。

### A4. 影響範囲
- `chordDegrees` / `chordForBar` / `deriveRoles` の起点（主和音）/ `allocateKeys` の結果（stable が変わるため）/ `chordMidiNotes`
- パッド・ベース・MIDI の pad/bass トラック・ステム accomp（同じ経路なので自動）
- **daylight+ionian・night+aeolian のミックスWAVは M1 基準と ±2LSB 以内であること**（完了条件）

## B. 完了（finished）状態で設定を変えられるようにする

- `finished` で **世界・スケール・クオンタイズ・seed・振り直し** を有効にする（`countin/playing/replay` だけ無効）。
- 設定を変えたら **キーボード地形の表示を即座に次テイクの配置に更新**する（`rebuildLayout`）。
- **完了パネルと書き出しは直前のテイクのまま**（`takeLog` は保持。ログ自身が `scaleId`・`quantize` を持つので書き出しは正しい）。
  パネルの音階名・クオンタイズ表示はテイクの値のまま（選択欄の現在値ではない）。
- 設定を変えた状態で「同じ配置でもう1テイク」は**現在の選択欄の設定**で開始する（ラベルは「もう1テイク」に統一してよい。「同じ配置で」が嘘になるため）。
- 「配置を振り直す」は従来どおり idle へ。

## C. 演奏のコツ（README に新設。本人の発見を原文の趣旨で）

```
## 演奏のコツ（本人の発見 2026-09-04）

- 同じキー付近を使い続けると、ループ感・楽曲感が出る。近いキーは近い音なので、モチーフの反復になる。
- 曲を展開したいときは、使うキーボードの位置を少しずつずらしていく。
- 普通の楽器でも同じことだが、ランダム配置でも同じ感覚が得られる。
```
アプリ内への表示（待機画面に1行）は本人判断待ち。今回は README のみ。

## D. テスト
1. N<7 の9音階すべてで、主和音が A3 の表と一致（度数・半音値）
2. N≥7 の12音階で `chordDegrees` と主和音が変更前と一致（回帰）
3. egyptian の stable が [1,2,4]、`allocateKeys` 合計26
4. `transition`: finished で設定変更しても状態は finished のまま（idle に落ちない）
5. 既存テスト不変。版表示 v0.7.1

## E. 完了条件
- テスト全通過、`node --check`、**ブラウザ console error 0**
- daylight+ionian の同一ログが M1 基準と ±2LSB 以内
- finished 状態でスケールを変えるとキーボード表示が変わり、書き出しは元のテイクのまま動く
